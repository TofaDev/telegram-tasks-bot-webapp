import * as Interfaces from '../../generated/telegram-interfaces';
import { Structure } from '../../types/interfaces';
/** This object represents a service message about an edited forum topic. */
export declare class ForumTopicReopened implements Structure {
    payload: Interfaces.TelegramForumTopicReopened;
    constructor(payload: Interfaces.TelegramForumTopicReopened);
    get [Symbol.toStringTag](): string;
    toJSON(): Interfaces.TelegramForumTopicReopened;
}

import * as Interfaces from '../../generated/telegram-interfaces';
import { Structure } from '../../types/interfaces';
import { InlineKeyboardButton } from './inline-keyboard-button';
/** This object represents an inline keyboard that appears right next to the message it belongs to. */
export declare class InlineKeyboardMarkup implements Structure {
    payload: Interfaces.TelegramInlineKeyboardMarkup;
    constructor(payload: Interfaces.TelegramInlineKeyboardMarkup);
    get [Symbol.toStringTag](): string;
    /** Array of button rows */
    get inlineKeyboard(): InlineKeyboardButton[][];
    toJSON(): Interfaces.TelegramInlineKeyboardMarkup;
}

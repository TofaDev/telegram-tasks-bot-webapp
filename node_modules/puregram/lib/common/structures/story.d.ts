import * as Interfaces from '../../generated/telegram-interfaces';
import { Structure } from '../../types/interfaces';
export declare class Story implements Structure {
    payload: Interfaces.TelegramStory;
    constructor(payload: Interfaces.TelegramStory);
    get [Symbol.toStringTag](): string;
    toJSON(): Interfaces.TelegramStory;
}

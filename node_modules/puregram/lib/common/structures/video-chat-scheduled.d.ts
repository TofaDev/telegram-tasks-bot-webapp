import * as Interfaces from '../../generated/telegram-interfaces';
import { Structure } from '../../types/interfaces';
/**
 * This object represents a service message about a video chat scheduled in the chat
 */
export declare class VideoChatScheduled implements Structure {
    payload: Interfaces.TelegramVideoChatScheduled;
    constructor(payload: Interfaces.TelegramVideoChatScheduled);
    get [Symbol.toStringTag](): string;
    /** Point in time (Unix timestamp) when the video chat is supposed to be started by a chat administrator */
    get startDate(): number;
    toJSON(): Interfaces.TelegramVideoChatScheduled;
}

import * as Interfaces from '../../generated/telegram-interfaces';
import { Structure } from '../../types/interfaces';
/** This object represents a service message about a user allowing a bot added to the attachment menu to write messages. Currently holds no information. */
export declare class WriteAccessAllowed implements Structure {
    payload: Interfaces.TelegramWriteAccessAllowed;
    constructor(payload: Interfaces.TelegramWriteAccessAllowed);
    get [Symbol.toStringTag](): string;
    /** Name of the Web App which was launched from a link */
    get webAppName(): string | undefined;
    toJSON(): Interfaces.TelegramWriteAccessAllowed;
}
